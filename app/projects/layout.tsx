import type React from "react"
export const metadata = {
  title: "Projects - Saleh Kamal",
  description: "Detailed project case studies and implementations",
}

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
